{% set class = (label_class ?? false) ? 'sublabel ' ~ label_class : 'sublabel' %}

<p class="{{ class }}">This field will only accept markdown. Mardown is simply a way of writing text that will be parsed and later turned into HTML markup. It allows the user to "create" HTML without having to know HTML. <a href="{{ path('j29.admin.markdown.index') }}">A full example of how to write with markdown can be found here.</a> <strong>Please note that <em>all HTML Tags</em> will be removed from this field if they are inserted.</strong> The user still has the ability to use many HTML elements such headings, links, lists, etc., though.</p>