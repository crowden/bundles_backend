{% set class = (label_class ?? false) ? 'sublabel ' ~ label_class : 'sublabel' %}

<p class="{{ class }}">Only choose another document if you wish to replace the one that is currently here. Choosing a different document will delete the current one.</p>